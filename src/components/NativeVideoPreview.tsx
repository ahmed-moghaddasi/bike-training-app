// Web stub — video preview is handled via <video> element in App.tsx on web.
// Metro resolves NativeVideoPreview.native.tsx for iOS/Android builds.
type Props = { uri: string; drillName: string; durationSeconds?: number };
export function NativeVideoPreview(_: Props) { return null; }
